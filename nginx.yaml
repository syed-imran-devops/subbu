- name: install and run nginx
  hosts: web
  become: yes      #equal to -b in adhoc commands, getting root access
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      state: latest



- name: hi subbu install nginx 
  hosts: web
  become: yes
  taks:
  - name: insall nginx
    ansible.builtin.dnf:
      name: nginx
      state: latest
